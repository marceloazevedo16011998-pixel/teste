# Google OAuth

Configuração necessária. Criar credenciais OAuth no Google Cloud para o domínio de produção e manter qualquer Client Secret somente no backend.

Não existe login Google falso neste projeto-base. O callback deve ser uma URL HTTPS real do backend publicado e cadastrada exatamente no provedor.
