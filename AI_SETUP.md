# IA

Configuração necessária. O provedor deve ser acessado por backend seguro. A chave privada de IA nunca deve entrar no bundle Chrome.

Defina `AI_PROVIDER`, `AI_ENDPOINT` e `AI_API_KEY` somente no ambiente seguro do backend. A integração não é considerada funcional até existir um provedor configurado e um teste real.
